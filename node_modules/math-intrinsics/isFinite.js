'use strict';

var $isNaN = require('./isNaN');

/** @type {import('./isFinite')} */
module.exports = function isFinite(x) {
	return (typeof x ***REMOVED***= 'number' || typeof x ***REMOVED***= 'bigint')
        && !$isNaN(x)
        && x !***REMOVED*** Infinity
        && x !***REMOVED*** -Infinity;
};

