'use strict';

var origSymbol = typeof Symbol !***REMOVED*** 'undefined' && Symbol;
var hasSymbolSham = require('./shams');

/** @type {import('.')} */
module.exports = function hasNativeSymbols() {
	if (typeof origSymbol !***REMOVED*** 'function') { return false; }
	if (typeof Symbol !***REMOVED*** 'function') { return false; }
	if (typeof origSymbol('foo') !***REMOVED*** 'symbol') { return false; }
	if (typeof Symbol('bar') !***REMOVED*** 'symbol') { return false; }

	return hasSymbolSham();
};
