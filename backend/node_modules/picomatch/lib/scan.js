'use strict';

const utils = require('./utils');
const {
  CHAR_ASTERISK,             /* * */
  CHAR_AT,                   /* @ */
  CHAR_BACKWARD_SLASH,       /* \ */
  CHAR_COMMA,                /* , */
  CHAR_DOT,                  /* . */
  CHAR_EXCLAMATION_MARK,     /* ! */
  CHAR_FORWARD_SLASH,        /* / */
  CHAR_LEFT_CURLY_BRACE,     /* { */
  CHAR_LEFT_PARENTHESES,     /* ( */
  CHAR_LEFT_SQUARE_BRACKET,  /* [ */
  CHAR_PLUS,                 /* + */
  CHAR_QUESTION_MARK,        /* ? */
  CHAR_RIGHT_CURLY_BRACE,    /* } */
  CHAR_RIGHT_PARENTHESES,    /* ) */
  CHAR_RIGHT_SQUARE_BRACKET  /* ] */
} = require('./constants');

const isPathSeparator = code => {
  return code ***REMOVED***= CHAR_FORWARD_SLASH || code ***REMOVED***= CHAR_BACKWARD_SLASH;
};

const depth = token => {
  if (token.isPrefix !***REMOVED*** true) {
    token.depth = token.isGlobstar ? Infinity : 1;
  }
};

/**
 * Quickly scans a glob pattern and returns an object with a handful of
 * useful properties, like `isGlob`, `path` (the leading non-glob, if it exists),
 * `glob` (the actual pattern), `negated` (true if the path starts with `!` but not
 * with `!(`) and `negatedExtglob` (true if the path starts with `!(`).
 *
 * ```js
 * const pm = require('picomatch');
 * console.log(pm.scan('foo/bar/*.js'));
 * { isGlob: true, input: 'foo/bar/*.js', base: 'foo/bar', glob: '*.js' }
 * ```
 * @param {String} `str`
 * @param {Object} `options`
 * @return {Object} Returns an object with tokens and regex source string.
 * @api public
 */

const scan = (input, options) => {
  const opts = options || {};

  const length = input.length - 1;
  const scanToEnd = opts.parts ***REMOVED***= true || opts.scanToEnd ***REMOVED***= true;
  const slashes = [];
  const tokens = [];
  const parts = [];

  let str = input;
  let index = -1;
  let start = 0;
  let lastIndex = 0;
  let isBrace = false;
  let isBracket = false;
  let isGlob = false;
  let isExtglob = false;
  let isGlobstar = false;
  let braceEscaped = false;
  let backslashes = false;
  let negated = false;
  let negatedExtglob = false;
  let finished = false;
  let braces = 0;
  let prev;
  let code;
  let token = { value: '', depth: 0, isGlob: false };

  const eos = () => index >= length;
  const peek = () => str.charCodeAt(index + 1);
  const advance = () => {
    prev = code;
    return str.charCodeAt(++index);
  };

  while (index < length) {
    code = advance();
    let next;

    if (code ***REMOVED***= CHAR_BACKWARD_SLASH) {
      backslashes = token.backslashes = true;
      code = advance();

      if (code ***REMOVED***= CHAR_LEFT_CURLY_BRACE) {
        braceEscaped = true;
      }
      continue;
    }

    if (braceEscaped ***REMOVED***= true || code ***REMOVED***= CHAR_LEFT_CURLY_BRACE) {
      braces++;

      while (eos() !***REMOVED*** true && (code = advance())) {
        if (code ***REMOVED***= CHAR_BACKWARD_SLASH) {
          backslashes = token.backslashes = true;
          advance();
          continue;
        }

        if (code ***REMOVED***= CHAR_LEFT_CURLY_BRACE) {
          braces++;
          continue;
        }

        if (braceEscaped !***REMOVED*** true && code ***REMOVED***= CHAR_DOT && (code = advance()) ***REMOVED***= CHAR_DOT) {
          isBrace = token.isBrace = true;
          isGlob = token.isGlob = true;
          finished = true;

          if (scanToEnd ***REMOVED***= true) {
            continue;
          }

          break;
        }

        if (braceEscaped !***REMOVED*** true && code ***REMOVED***= CHAR_COMMA) {
          isBrace = token.isBrace = true;
          isGlob = token.isGlob = true;
          finished = true;

          if (scanToEnd ***REMOVED***= true) {
            continue;
          }

          break;
        }

        if (code ***REMOVED***= CHAR_RIGHT_CURLY_BRACE) {
          braces--;

          if (braces ***REMOVED***= 0) {
            braceEscaped = false;
            isBrace = token.isBrace = true;
            finished = true;
            break;
          }
        }
      }

      if (scanToEnd ***REMOVED***= true) {
        continue;
      }

      break;
    }

    if (code ***REMOVED***= CHAR_FORWARD_SLASH) {
      slashes.push(index);
      tokens.push(token);
      token = { value: '', depth: 0, isGlob: false };

      if (finished ***REMOVED***= true) continue;
      if (prev ***REMOVED***= CHAR_DOT && index ***REMOVED***= (start + 1)) {
        start += 2;
        continue;
      }

      lastIndex = index + 1;
      continue;
    }

    if (opts.noext !***REMOVED*** true) {
      const isExtglobChar = code ***REMOVED***= CHAR_PLUS
        || code ***REMOVED***= CHAR_AT
        || code ***REMOVED***= CHAR_ASTERISK
        || code ***REMOVED***= CHAR_QUESTION_MARK
        || code ***REMOVED***= CHAR_EXCLAMATION_MARK;

      if (isExtglobChar ***REMOVED***= true && peek() ***REMOVED***= CHAR_LEFT_PARENTHESES) {
        isGlob = token.isGlob = true;
        isExtglob = token.isExtglob = true;
        finished = true;
        if (code ***REMOVED***= CHAR_EXCLAMATION_MARK && index ***REMOVED***= start) {
          negatedExtglob = true;
        }

        if (scanToEnd ***REMOVED***= true) {
          while (eos() !***REMOVED*** true && (code = advance())) {
            if (code ***REMOVED***= CHAR_BACKWARD_SLASH) {
              backslashes = token.backslashes = true;
              code = advance();
              continue;
            }

            if (code ***REMOVED***= CHAR_RIGHT_PARENTHESES) {
              isGlob = token.isGlob = true;
              finished = true;
              break;
            }
          }
          continue;
        }
        break;
      }
    }

    if (code ***REMOVED***= CHAR_ASTERISK) {
      if (prev ***REMOVED***= CHAR_ASTERISK) isGlobstar = token.isGlobstar = true;
      isGlob = token.isGlob = true;
      finished = true;

      if (scanToEnd ***REMOVED***= true) {
        continue;
      }
      break;
    }

    if (code ***REMOVED***= CHAR_QUESTION_MARK) {
      isGlob = token.isGlob = true;
      finished = true;

      if (scanToEnd ***REMOVED***= true) {
        continue;
      }
      break;
    }

    if (code ***REMOVED***= CHAR_LEFT_SQUARE_BRACKET) {
      while (eos() !***REMOVED*** true && (next = advance())) {
        if (next ***REMOVED***= CHAR_BACKWARD_SLASH) {
          backslashes = token.backslashes = true;
          advance();
          continue;
        }

        if (next ***REMOVED***= CHAR_RIGHT_SQUARE_BRACKET) {
          isBracket = token.isBracket = true;
          isGlob = token.isGlob = true;
          finished = true;
          break;
        }
      }

      if (scanToEnd ***REMOVED***= true) {
        continue;
      }

      break;
    }

    if (opts.nonegate !***REMOVED*** true && code ***REMOVED***= CHAR_EXCLAMATION_MARK && index ***REMOVED***= start) {
      negated = token.negated = true;
      start++;
      continue;
    }

    if (opts.noparen !***REMOVED*** true && code ***REMOVED***= CHAR_LEFT_PARENTHESES) {
      isGlob = token.isGlob = true;

      if (scanToEnd ***REMOVED***= true) {
        while (eos() !***REMOVED*** true && (code = advance())) {
          if (code ***REMOVED***= CHAR_LEFT_PARENTHESES) {
            backslashes = token.backslashes = true;
            code = advance();
            continue;
          }

          if (code ***REMOVED***= CHAR_RIGHT_PARENTHESES) {
            finished = true;
            break;
          }
        }
        continue;
      }
      break;
    }

    if (isGlob ***REMOVED***= true) {
      finished = true;

      if (scanToEnd ***REMOVED***= true) {
        continue;
      }

      break;
    }
  }

  if (opts.noext ***REMOVED***= true) {
    isExtglob = false;
    isGlob = false;
  }

  let base = str;
  let prefix = '';
  let glob = '';

  if (start > 0) {
    prefix = str.slice(0, start);
    str = str.slice(start);
    lastIndex -= start;
  }

  if (base && isGlob ***REMOVED***= true && lastIndex > 0) {
    base = str.slice(0, lastIndex);
    glob = str.slice(lastIndex);
  } else if (isGlob ***REMOVED***= true) {
    base = '';
    glob = str;
  } else {
    base = str;
  }

  if (base && base !***REMOVED*** '' && base !***REMOVED*** '/' && base !***REMOVED*** str) {
    if (isPathSeparator(base.charCodeAt(base.length - 1))) {
      base = base.slice(0, -1);
    }
  }

  if (opts.unescape ***REMOVED***= true) {
    if (glob) glob = utils.removeBackslashes(glob);

    if (base && backslashes ***REMOVED***= true) {
      base = utils.removeBackslashes(base);
    }
  }

  const state = {
    prefix,
    input,
    start,
    base,
    glob,
    isBrace,
    isBracket,
    isGlob,
    isExtglob,
    isGlobstar,
    negated,
    negatedExtglob
  };

  if (opts.tokens ***REMOVED***= true) {
    state.maxDepth = 0;
    if (!isPathSeparator(code)) {
      tokens.push(token);
    }
    state.tokens = tokens;
  }

  if (opts.parts ***REMOVED***= true || opts.tokens ***REMOVED***= true) {
    let prevIndex;

    for (let idx = 0; idx < slashes.length; idx++) {
      const n = prevIndex ? prevIndex + 1 : start;
      const i = slashes[idx];
      const value = input.slice(n, i);
      if (opts.tokens) {
        if (idx ***REMOVED***= 0 && start !***REMOVED*** 0) {
          tokens[idx].isPrefix = true;
          tokens[idx].value = prefix;
        } else {
          tokens[idx].value = value;
        }
        depth(tokens[idx]);
        state.maxDepth += tokens[idx].depth;
      }
      if (idx !***REMOVED*** 0 || value !***REMOVED*** '') {
        parts.push(value);
      }
      prevIndex = i;
    }

    if (prevIndex && prevIndex + 1 < input.length) {
      const value = input.slice(prevIndex + 1);
      parts.push(value);

      if (opts.tokens) {
        tokens[tokens.length - 1].value = value;
        depth(tokens[tokens.length - 1]);
        state.maxDepth += tokens[tokens.length - 1].depth;
      }
    }

    state.slashes = slashes;
    state.parts = parts;
  }

  return state;
};

module.exports = scan;
