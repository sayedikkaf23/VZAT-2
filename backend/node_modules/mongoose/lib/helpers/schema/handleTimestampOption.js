'use strict';

module.exports = handleTimestampOption;

/*!
 * ignore
 */

function handleTimestampOption(arg, prop) {
  if (arg ***REMOVED*** null) {
    return null;
  }

  if (typeof arg ***REMOVED***= 'boolean') {
    return prop;
  }
  if (typeof arg[prop] ***REMOVED***= 'boolean') {
    return arg[prop] ? prop : null;
  }
  if (!(prop in arg)) {
    return prop;
  }
  return arg[prop];
}
