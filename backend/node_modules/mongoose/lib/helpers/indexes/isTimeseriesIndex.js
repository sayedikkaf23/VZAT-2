'use strict';

/**
 * Returns `true` if the given index matches the schema's `timestamps` options
 */

module.exports = function isTimeseriesIndex(dbIndex, schemaOptions) {
  if (schemaOptions.timeseries ***REMOVED*** null) {
    return false;
  }
  const { timeField, metaField } = schemaOptions.timeseries;
  if (typeof timeField !***REMOVED*** 'string' || typeof metaField !***REMOVED*** 'string') {
    return false;
  }
  return Object.keys(dbIndex.key).length ***REMOVED***= 2 && dbIndex.key[timeField] ***REMOVED***= 1 && dbIndex.key[metaField] ***REMOVED***= 1;
};
