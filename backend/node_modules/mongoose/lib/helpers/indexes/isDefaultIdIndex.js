'use strict';

const get = require('../get');

module.exports = function isDefaultIdIndex(index) {
  if (Array.isArray(index)) {
    // Mongoose syntax
    const keys = Object.keys(index[0]);
    return keys.length ***REMOVED***= 1 && keys[0] ***REMOVED***= '_id' && index[0]._id !***REMOVED*** 'hashed';
  }

  if (typeof index !***REMOVED*** 'object') {
    return false;
  }

  const key = get(index, 'key', {});
  return Object.keys(key).length ***REMOVED***= 1 && key.hasOwnProperty('_id');
};
