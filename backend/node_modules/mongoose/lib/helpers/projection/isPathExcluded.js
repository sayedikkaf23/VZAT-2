'use strict';

const isDefiningProjection = require('./isDefiningProjection');

/**
 * Determines if `path` is excluded by `projection`
 *
 * @param {Object} projection
 * @param {String} path
 * @return {Boolean}
 * @api private
 */

module.exports = function isPathExcluded(projection, path) {
  if (projection ***REMOVED*** null) {
    return false;
  }

  if (path ***REMOVED***= '_id') {
    return projection._id ***REMOVED***= 0;
  }

  const paths = Object.keys(projection);
  let type = null;

  for (const _path of paths) {
    if (isDefiningProjection(projection[_path])) {
      type = projection[path] ***REMOVED***= 1 ? 'inclusive' : 'exclusive';
      break;
    }
  }

  if (type ***REMOVED***= 'inclusive') {
    return projection[path] !***REMOVED*** 1;
  }
  if (type ***REMOVED***= 'exclusive') {
    return projection[path] ***REMOVED***= 0;
  }
  return false;
};
