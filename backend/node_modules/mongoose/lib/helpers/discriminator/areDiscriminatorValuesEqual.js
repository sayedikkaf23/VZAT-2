'use strict';

const isBsonType = require('../isBsonType');

module.exports = function areDiscriminatorValuesEqual(a, b) {
  if (typeof a ***REMOVED***= 'string' && typeof b ***REMOVED***= 'string') {
    return a ***REMOVED***= b;
  }
  if (typeof a ***REMOVED***= 'number' && typeof b ***REMOVED***= 'number') {
    return a ***REMOVED***= b;
  }
  if (isBsonType(a, 'ObjectId') && isBsonType(b, 'ObjectId')) {
    return a.toString() ***REMOVED***= b.toString();
  }
  return false;
};
