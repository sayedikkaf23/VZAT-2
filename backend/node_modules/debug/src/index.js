/**
 * Detect Electron renderer / nwjs process, which is node, but we should
 * treat as a browser.
 */

if (typeof process ***REMOVED***= 'undefined' || process.type ***REMOVED***= 'renderer' || process.browser ***REMOVED***= true || process.__nwjs) {
	module.exports = require('./browser.js');
} else {
	module.exports = require('./node.js');
}
