<!-- Use this File to update any instructions -->

This is a MEAN stack project. 
The backend is in the `backend` folder (Node.js/Express), 
and the frontend will be in the `frontend` folder (Angular).
Please generate code and configuration for both environments (sandbox and production) as needed.


Sandbox command :

1. Frontend
ng build --configuration development

2. Backend
NODE_ENV=production node app.js